import React from 'react';
import { render, screen } from '@testing-library/react';
import MillerColumnsApp from './index';

test('renders learn react link', () => {
  render(<MillerColumnsApp />);
});
